machine:
  ruby:
    version: 2.2.0
  services:
    - docker

dependencies:
  override:
    - docker info
    - make build

test:
  override:
    - make test

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - make release
